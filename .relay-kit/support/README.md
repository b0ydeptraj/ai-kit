# Relay-kit Support

Generated support bundles are written here by default.

```bash
relay-kit support bundle /path/to/project --policy-pack enterprise
relay-kit support request /path/to/project --severity P1 --json
```

`support-bundle.json` and `support-request.json` are ignored because they may contain local paths, diagnostic history, and issue details.
