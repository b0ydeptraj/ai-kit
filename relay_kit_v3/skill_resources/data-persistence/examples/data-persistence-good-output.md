# data-persistence Good Output Example

Request: Use `data-persistence` for a data persistence lane where the user gave a compact request.

Good response shape:

- Recommended skill: `data-persistence` because the request matches `persistence-support` work.
- Read first: model files, repository or DAO code, migration files.
- Evidence gathered: list exact files, command output, docs, or state artifacts inspected.
- Output: .relay-kit/references/data-persistence.md.
- Residual risk: name what is still unverified and the smallest next check.
- Handoff: architect.

This is good because it is anchored to project evidence, limits the claim, and makes the next action executable.
