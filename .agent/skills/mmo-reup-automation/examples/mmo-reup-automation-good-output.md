# mmo-reup-automation Good Output

Good output models the workbench: inventory table, dedupe ledger, publish queue, review drawer, and rollback evidence.

It includes dry-run output with skipped duplicates, throttle reasons, attribution status, and operator approval state.
