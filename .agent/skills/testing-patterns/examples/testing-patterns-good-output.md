# testing-patterns Good Output Example

Request: Use `testing-patterns` for a testing patterns lane where the user gave a compact request.

Good response shape:

- Recommended skill: `testing-patterns` because the request matches `quality-support` work.
- Read first: test folders, test config, fixtures or factories.
- Evidence gathered: list exact files, command output, docs, or state artifacts inspected.
- Output: .relay-kit/references/testing-patterns.md.
- Residual risk: name what is still unverified and the smallest next check.
- Handoff: developer.

This is good because it is anchored to project evidence, limits the claim, and makes the next action executable.
