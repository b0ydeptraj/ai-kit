# api-integration Good Output Example

Request: Use `api-integration` for a api integration lane where the user gave a compact request.

Good response shape:

- Recommended skill: `api-integration` because the request matches `integration-support` work.
- Read first: HTTP or RPC client code, settings or secret config, test or mock code.
- Evidence gathered: list exact files, command output, docs, or state artifacts inspected.
- Output: .relay-kit/references/api-integration.md.
- Residual risk: name what is still unverified and the smallest next check.
- Handoff: architect.

This is good because it is anchored to project evidence, limits the claim, and makes the next action executable.
